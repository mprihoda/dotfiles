SDKMAN_INIT="${HOME}/.sdkman/bin/sdkman-init.sh"
if [ -e "${SDKMAN_INIT}" ]; then source "${SDKMAN_INIT}"; fi
#if [ -d "${HOME}/.jenv/bin" ]; then export PATH="$HOME/.jenv/bin:$PATH"; fi
#if which jenv > /dev/null; then eval "$(jenv init - --no-rehash)"; fi
